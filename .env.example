# Copy to .env for local development. Never commit secrets.

# Backend
PORT=3000

# Configure only after the approved corporate identity and document services are known.
CORPORATE_AUTH_ISSUER=
DOCUMENT_EXTRACTION_ENDPOINT=
DOCUMENT_EXTRACTION_API_KEY=
